@uniform(matA: Mat3)
@uniform(matB: Mat3)
@uniform(arr: Float[10])

// main function
func main() {
    var a0 = uniform.arr;
    var a1 = a0[0];

    var v0 = vec3(1.0, 2.0, 3.0);
    var v1 = v0.y;
    var v2 = v0[1];

    var m0 = uniform.matA;
    var mr0 = m0[0];
    var mrc0 = m0[0][0];

    var row = 1;
    var col = 2;
    var mrc1 = m0[row][col];
}