func multiply(x, y) -> x * y;
func squared(x) -> x * x;
func halfOf(x) -> x / 2;

func main() {
	var x = multiply(1, 2)->halfOf(_)->multiply(_, 3)->squared(_);
	var y = 5 / 2;
	var a1 = 5 / 2.0;
	var a2 = 2.0 / 5;
	var a3 = 2.0 / 5.0;
	var v1 = (1,2) / 2;
	var v2 = 2 / (1,2);
	var v3 = (1,2) / (3,4);

    var v = 5 / 2;
}