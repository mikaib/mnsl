@uniform(matA: Mat3)
@uniform(matB: Mat3)
@uniform(arr: Float[10])

// main function
func main() {
    var a0 = uniform.arr;
    var a1 = a0[0];

    var v0 = vec3(1.0, 2.0, 3.0);
    var v1 = v0.y;
    var v2 = v0[1];
}