#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

fragment void main0()
{
    float3 v = float3(float(1));
    float3 _43 = float3(float(1), float(2), float(3));
    v = float3(_43.z, _43.y, _43.x);
}

