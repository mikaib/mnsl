#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

fragment void main0(texture2d<float> u_Texture [[texture(0)]], sampler u_TextureSmplr [[sampler(0)]])
{
    float2 x = float2(float(2)) / float2(int2(u_Texture.get_width(), u_Texture.get_height()));
}

